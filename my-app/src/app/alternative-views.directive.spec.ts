import { AlternativeViewsDirective } from './alternative-views.directive';

describe('AlternativeViewsDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternativeViewsDirective();
    expect(directive).toBeTruthy();
  });
});
